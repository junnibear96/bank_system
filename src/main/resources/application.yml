server:
  port: ${PORT:8080}

spring:
  datasource:
    # Hardcoded Railway Public URL as requested by user
    url: jdbc:mysql://yamanote.proxy.rlwy.net:26416/railway?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: LhkJiSjiBWnkbvzElUCEFKGtDiYOmaci
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

